{
  "name": "totals-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Dependencies for the Netlify sync function. The PWA itself is plain static files and needs no build step.",
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
